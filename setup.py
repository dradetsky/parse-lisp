from setuptools import setup, find_packages

install_requires = (
    'sly'
)

setup(
    name='parse_lisp',
    version='0.0.0',
    description='parse_lisp',
    author='dmr',
    author_email='dradetsky@gmail.com',
    packages=['parse_lisp'],
    install_requires=install_requires,
    license='WTFPL',
)
